"""
Entry point for the financial-study LangGraph agent system.

Steps to implement:
  1. Load settings (config.settings)
  2. Build the compiled graph (graph.builder)
  3. Construct the initial input state
  4. Invoke (or stream) the graph
  5. Handle / display the final output state
"""
